name: Deploy to Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build Docker image
        run: |
          cd ./auth-service/auth_service && docker build -t menoitami/backend_service.api:latest --no-cache .

      - name: Push Docker image to Docker Hub
        run: |
          docker push menoitami/backend_service.api:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST_IP }}
          username: ${{ secrets.USER_NAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd OpenID-provider/auth-service/
            git pull origin main
            docker compose down
            docker compose pull
            docker compose build --no-cache
            docker compose up -d --force-recreate
            docker compose logs -f | head -n 50  # Вывод первых 50 строк логов